apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: beauty-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: beauty-service
            port:
              number: 8080
