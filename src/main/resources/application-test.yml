spring:
  flyway:
    enabled: true
    locations: classpath:db/migration/postgresql

  jpa:
    hibernate:
      ddl-auto: none

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:}
            client-secret: ${GOOGLE_SECRET:}
            scope: profile, email

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: beautyapplicationilias@gmail.com
    password: kfna sibw nutu ephb
    properties:
      mail:
        smtp:
          ssl:
            trust: smtp.gmail.com
          auth: true
          starttls:
            enable: true

  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY:}
      chat:
        options:
          model: claude-sonnet-4-6

jwt:
  publicKey: classpath:public-key.pem
  privateKey: classpath:pkcs8.key

aws:
  accessKey: ${AWS_ACCESS_KEY:}
  secretKey: ${AWS_SECRET_KEY:}

google:
  accessKey: ${GOOGLE_ACCESS_KEY}

stripe:
  secretKey: ${STRIPE_SECRET_KEY}
cors:
  url: http://localhost:4200