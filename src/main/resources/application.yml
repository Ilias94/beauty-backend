management:
  endpoints:
    web:
      exposure:
        include: '*'
server:
  port: 8082
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/beautypg}
    username: ${DB_USERNAME:beauty}
    password: ${DB_PASSWORD:beauty}
    hikari:
      connection-init-sql: "SET search_path TO beautypg, public"
  jackson:
    serialization:
      fail-on-empty-beans: false
  flyway:

    enabled: true
    locations: classpath:db/migration/postgresql
    schemas: beautypg
    placeholder-replacement: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          ssl:
            trust: smtp.gmail.com
          auth: true
          starttls:
            enable: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_SECRET}

  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          model: claude-sonnet-4-6

  jpa:
    hibernate:
      ddl-auto: none
      default_schema: beautypg
    properties:
      hibernate.default_schema: beautypg

jwt:
  privateKey: ${JWT_PRIVATE_KEY}
  publicKey: ${JWT_PUBLIC_KEY}

aws:
  accessKey: ${AWS_ACCESS_KEY}
  secretKey: ${AWS_SECRET_KEY}

google:
  accessKey: ${GOOGLE_ACCESS_KEY}

stripe:
  secretKey: ${STRIPE_SECRET_KEY}

hazelcast:
  network:
    rest-api:
      enabled: true
cors:
  url: https://beauty-front.onrender.com/

logging:
  level:
    org.flywaydb: DEBUG

